export default defineAppConfig({
    ui: {
        primary: "sky",
        gray: "cool",
        input: {
            default: {
                size: "md"
            }
        }
    }
});
