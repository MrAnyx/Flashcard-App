export default defineAppConfig({
    ui: {
        primary: "sky",
        gray: "cool",
        kbd: {
            padding: "px-1.5"
        },
        input: {
            default: {
                size: "md"
            }
        },
        select: {
            default: {
                size: "md"
            }
        },
        table: {
            td: {
                base: "max-w-[0] truncate"
            }
        },
    },
});
