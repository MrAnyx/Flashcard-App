import schedule from "node-schedule";

export default defineNuxtPlugin(() =>
{
    // schedule.scheduleJob(updateCardsToReview.name, updateCardsToReview.schedule, updateCardsToReview.handler);
});
